{% extends 'base.html.twig' %}

{% block body %}
<div class="restricted_container">
  <h5><i>Le {{event.date | date("d m Y")}}</i></h5>
  <h2>{{event.title}} <i> ({{event.subscriber|length}})</i></h2>

  <table>
    <tr>
      <th>Prénom</th>
      <th>Nom</th>
    </tr>
    {% for subscriber in event.subscriber %}
    <tr>
      <td>{{subscriber.firstname}}</td>
      <td>{{subscriber.name}}</td>
    </tr>

    {% endfor %}
  </table>
  <br>
  <a href="{{path('member_event_index')}}" class="btn btn-primary"><i class="fa fa-rotate-left"></i></a>
</div>
{% endblock %}
